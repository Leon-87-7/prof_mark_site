import { defineCollection, z } from 'astro:content';

// Define collections here if you want to use Astro Content Collections
// Example:
// const blog = defineCollection({
//   type: 'content',
//   schema: z.object({
//     title: z.string(),
//     description: z.string(),
//     pubDate: z.date(),
//   }),
// });

// export const collections = { blog };

// For now, export an empty collections object
export const collections = {};
